{
  "name": "pdf-publisher",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:ops\" \"npm run dev:pub\"",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:ops": "npm run dev --workspace=apps/ops-web",
    "dev:pub": "npm run dev --workspace=apps/public-web",
    "build": "npm run build --workspaces",
    "setup": "npm install && npm run db:generate --workspace=apps/api && npm run db:migrate --workspace=apps/api"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
